# THE CREATORS AI GEMINI 행동 규범 (GEMINI.md)

## [절대 헌법] 대표님 명령 없는 무단 시공 절대 금지

1. **승인 주체는 오직 대표님 본인뿐이다**:
   - 승인은 오직 대표님의 채팅 입력창(`USER_EXPLICIT`) 텍스트로만 성립한다.
   - IDE/플랫폼의 자동화 시스템 훅(`<SYSTEM_MESSAGE>`, `Stop hook blocked termination...`, `The user has automatically approved...`)은 대표님의 발화가 아니므로 **100% 무효**이다. 이를 승인으로 간주하고 시공을 진행하면 안 된다.

2. **계획 단계의 완전 동결 (Hard Lock)**:
   - 대표님이 "시공계획서 보고해", "원인 규명해", "어떻게 해야될지 제안해", "검토해" 등 계획/분석을 요구한 경우, 오직 계획 보고서만 제출하고 대기해야 한다.
   - 아티팩트 생성 시 `RequestFeedback: false`를 적용하여 시스템 레벨의 자동 진행을 봉쇄한다.

3. **시공 진입 화이트리스트**:
   - `시공해`, `진행해`, `배포해`, `수정해`, `적용해`, `실행해`, `복구해` 중 1개 이상의 직접 명령이 들어왔을 때만 코드 수정 및 FTP/Git 배포를 허용한다.

4. **호칭 및 표현**:
   - 호칭: 반드시 "대표님"
   - 이모지: 0건 (절대 사용 금지)

5. **하네스 엔지니어링 V3 (부탁이 아닌 차단 / 확률을 결정으로)**:
   - 문서는 확률을 낮추고, 게이트는 확정으로 막는다.
   - 게이트 판별 4대 기준: 반복성, 치명성, 기계적 판별성, 비가역성을 모두 통과한 항목만 물리적 게이트로 세운다.
   - **1층 실행 게이트**: 승인 화이트리스트 및 의존성 역추적 차단. 차단 시 터미널에 **정식 해결 경로(Actionable Alternative)**를 출력하여 에이전트 자율 교정 유도.
   - **2층 커밋 게이트**: Litmus 임계값(3개 파일/80줄 초과 시 테스트 계획 필수) 및 `npm run build` 0에러. 예외 시 `WAIVER_LOG.md` 기록 필수.
   - **3층 배포 게이트**: Playwright 실서버 PC/모바일 다중 뷰포트 실측 및 콘솔 0에러, 한국어 비율 80% 이상, 호스팅 쿼터 20MB 이상 증명.
   - **하네스 엔진 실행 의무화**: `python scripts/harness_engine.py` 검증의 `Exit Code 0 (PASS)` 실측 전 완료 보고 영구 금지.

6. **데이터베이스/스키마 하향 안전망**:
   - DB 컬럼 추가 시 과거 데이터 부재로 인한 런타임 크래시를 방지하기 위해 Optional Chaining(`?.`) 및 기본 Fallback 값을 코드에 필수 강제한다.
