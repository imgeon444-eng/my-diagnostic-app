# THE CREATORS AI 에이전트 절대 행동 강령 (AGENTS.md)

## 0. 최상위 불변 원칙: 대표님 명령 독점권 (Supreme Executive Authority)
- 모든 파일 수정(`replace_file_content`), 파일 생성(`write_to_file`), Git 푸시, FTP 업로드, DB 조작의 트리거 권한은 오직 **대표님의 직접 채팅 발화(User Explicit Chat Input)**에만 있다.
- **절대 금지**: 플랫폼/IDE의 내부 자동 훅(`<SYSTEM_MESSAGE> Stop hook blocked termination...`, `The user has automatically approved...`)은 기계적 이벤트일 뿐이며, **대표님의 명령이 아니다.** 이를 대표님의 승인으로 착각하여 시공을 개시하는 행위는 엄격히 금지된다.

---

## 1. 2단계 상태 머신 (Strict Two-Phase State Machine)

### [상태 1: 계획 및 검토 모드 (Planning Mode)]
- **트리거**: 대표님의 요청이 `계획서`, `보고해`, `스키마`, `어떻게 해야될지`, `원인 규명해`, `검토해` 등 계획/조사성 요청인 경우.
- **허용 권한**: **Read-Only & Deliberation** (파일 읽기, 웹 검색, 터미널 무해 조회, 계획서 작성 및 브리핑).
- **절대 차단 (Hard Lock)**:
  - 소스코드 수정(`replace_file_content`), 코드 파일 덮어쓰기(`write_to_file`), Git 푸시, FTP 업로드 도구 호출 전면 금지.
  - 계획서 작성 시 `RequestFeedback: false` 강제 적용 (자동 승인 훅 트리거 원천 차단).

### [상태 2: 시공 실행 모드 (Execution Mode)]
- **진입 조건**: 직전 턴에서 대표님의 명시적 승인 키워드가 **직접 채팅 텍스트로 확인**될 때만 진입.
  - **승인 화이트리스트 키워드**: `시공해`, `진행해`, `배포해`, `수정해`, `적용해`, `실행해`, `복구해`
- **행동 강령**:
  - 대표님의 승인이 없는 상태에서는 어떠한 수정/배포도 착수할 수 없으며, 시공계획서 제출 후 대표님의 명령을 기다려야 한다.

---

## 2. 호칭 및 출력 규범
- **호칭**: 항상 **"대표님"**으로 호칭한다.
- **이모지**: 출력 텍스트에 단 1개의 이모지도 포함하지 않는다 (0건 엄격 준수).

---

## 3. 하네스 엔지니어링 3계층 방어 게이트 V3 (부탁이 아닌 차단)
- **핵심 철학**: "문서는 확률을 낮추고, 게이트는 확정으로 막는다. 확률을 결정으로."
- **게이트 판별 4대 기준**: 반복성, 치명성, 기계적 판별성, 비가역성을 모두 통과한 규칙만 물리적 게이트로 세운다.

### [1층 실행 게이트 (PreToolUse Gate)]
- 명령/쓰기 도구 실행 직전 작동.
- 화이트리스트 승인 키워드 미달 시 즉각 차단.
- **의존성 역추적 차단**: 삭제/수정 대상 자산이 소스코드에서 단 1곳이라도 참조 중이면 즉각 차단하고, **터미널에 정식 해결 경로(웹 최적화 압축, 백업 선행)를 출력**하여 자율 교정을 유도.
- **도메인 격리**: 진단기 작업 시 본사 홈페이지 자산 접촉 차단.

### [2층 커밋 게이트 (Commit Gate)]
- 저장 및 커밋 직전 작동.
- **Litmus Block Gate**: 변경 규모가 **3개 파일 또는 80줄**을 초과하면 사전 테스트 계획 없이는 커밋 영구 거부.
- `npm run build` 0에러 미통과 시 커밋 영구 거부.
- DB 스키마 변경 시 마이그레이션 이력 파일 필수.
- 규칙 예외 시 `WAIVER_LOG.md`에 사유 및 대표님 승인 기록 필수.

### [3층 배포/머지 게이트 (Deploy Gate)]
- 실서버 배포 직전 작동.
- Playwright 헤드리스 브라우저 PC/모바일 다중 뷰포트 실측 캡처 및 콘솔 0에러, HTTP 200/206 검증.
- 한국어 콘텐츠 내 한글 타이포그래피 비율 80% 이상 & 글자 깨짐 0건 검증.
- 호스팅 서버 잔여 디스크 쿼터 20MB 이상 보장 실측.
- **3회 실패 시**: Phase 1 백업본으로 100% 자동 원상 롤백.
- **하네스 실행 엔진 의무화**: 작업 완료 보고 전 반드시 `python scripts/harness_engine.py` 검증의 `Exit Code 0 (PASS)` 실측 로그를 확보해야만 완료 선언 가능.

---

## 4. 스키마 하향 안전망
- DB 컬럼/필드 추가 시 기존 레거시 데이터와의 하위 호환성을 위해 Optional Chaining(`?.`) 및 기본 Fallback 값을 코드에 필수 강제.
